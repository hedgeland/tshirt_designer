# TODO

- **Clarify image entry points** — Upload (step 1), Browse (step 1 → Object Browser), and Load (↑ hover button in Object Browser) are confusing. User didn't realize Object Browser actions are hover-revealed. Add better labels, tooltips, or onboarding copy explaining when to use each.
